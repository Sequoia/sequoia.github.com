include ./menu_items.jade

mixin menuitem(name, url)
  //- todo: set current item? just not enough menu items rn
  if '/' + slug + '/' == url
    li.current: span= name
  else
    li: a(href=url)= name

nav
  ul
    each item in menu
      +menuitem(item.name, item.url)
      //- h1= slug
.clearfix